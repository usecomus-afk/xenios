name: Build Xenios iOS App (.ipa for Sideloadly)

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      server_url:
        description: 'Xenios Canli Sunucu URL (Orn: https://xenios-production.up.railway.app - Bos birakilirsa varsayilan kullanilir)'
        required: false
        default: ''
      create_release:
        description: 'GitHub Releases uzerinde surum yayinla'
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  build-ipa:
    name: Build Xenios iOS IPA
    runs-on: macos-14

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Node Dependencies
        run: |
          npm install

      - name: Configure Server URL (if specified)
        run: |
          if [ -n "${{ inputs.server_url }}" ]; then
            echo "Custom Server URL specified: ${{ inputs.server_url }}"
            echo "CAPACITOR_SERVER_URL=${{ inputs.server_url }}" >> $GITHUB_ENV
          fi

      - name: Sync Capacitor iOS Platform
        run: |
          npx cap sync ios

      - name: Select Xcode
        run: |
          sudo xcode-select -switch /Applications/Xcode_15.4.app || true
          xcodebuild -version

      - name: Build iOS App (.app)
        run: |
          if [ -d "ios/App/App.xcworkspace" ]; then
            PROJECT_ARG="-workspace ios/App/App.xcworkspace"
          else
            PROJECT_ARG="-project ios/App/App.xcodeproj"
          fi
          xcodebuild \
            $PROJECT_ARG \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package Payload into unsigned .ipa
        run: |
          mkdir -p build/ipa/Payload
          APP_PATH=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name "*.app" | head -n 1)
          echo "Found app at: $APP_PATH"
          cp -r "$APP_PATH" build/ipa/Payload/
          cd build/ipa
          zip -r -9 ../../Xenios.ipa Payload
          cd ../..
          ls -lh Xenios.ipa

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Xenios-iOS-IPA
          path: Xenios.ipa
          retention-days: 30

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && inputs.create_release == true)
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'Xenios Istanbul iOS App (Sideloadly IPA)' }}
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'v1.0.0-ios' }}
          files: Xenios.ipa
          body: |
            ## 🏛️ Xenios Istanbul iOS Mobil Uygulaması (.ipa)
            
            Bu sürüm **Sideloadly** veya benzeri sideload araçları (AltStore, TrollStore, Scarlet) ile iPhone / iPad cihazlara doğrudan yüklenmek üzere hazırlanmıştır.
            
            ### 🚀 Kurulum Adımları (Sideloadly):
            1. Yukarıdaki `Xenios.ipa` dosyasını bilgisayarınıza indirin.
            2. Bilgisayarınızda [Sideloadly](https://sideloadly.io/) programını açın ve iPhone'unuzu kablo ile bağlayın.
            3. İndirdiğiniz `Xenios.ipa` dosyasını Sideloadly penceresine sürükleyip bırakın.
            4. Apple ID e-postanızı girin ve **Start** butonuna tıklayın.
            5. Yükleme tamamlandıktan sonra iPhone'unuzda:
               - **Ayarlar > Genel > VPN ve Aygıt Yönetimi** bölümüne gidin.
               - Kendi Apple ID'nizi seçin ve **Güven ("Trust")** seçeneğine dokunun.
               - (iOS 16+ için) **Ayarlar > Gizlilik ve Güvenlik > Geliştirici Modu**'nu aktif edin ve telefonu yeniden başlatın.
            6. Xenios uygulamasını açıp kullanmaya başlayın!
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
